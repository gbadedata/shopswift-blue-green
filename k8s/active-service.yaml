apiVersion: v1
kind: Service
metadata:
  name: shopswift-active-service
  namespace: ecommerce-bluegreen
  labels:
    app: shopswift
    role: active
spec:
  type: ClusterIP
  selector:
    app: shopswift
    environment: blue
  ports:
    - name: http
      port: 80
      targetPort: 3000
